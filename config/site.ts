export type SiteConfig = typeof siteConfig;

let nav: {label: string, href: string}[] = [
  // {
  //   label: "Home",
  //   href: "/",
  // },
]

export const siteConfig = {
  name: "Telescope",
  navItems: nav,
  navMenuItems: nav,
  links: {
  },
};
